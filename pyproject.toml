[project]
name = "word2latex"
version = "0.1.0"
description = "Word (.docx) to Journal-Specific LaTeX Converter"
requires-python = ">=3.10"
dependencies = [
    "python-docx",
    "jinja2",
    "pyyaml",
]

[tool.setuptools.packages.find]
where = ["."]
